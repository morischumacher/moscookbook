/** The recipe form's field and label look, shared by its parts. */
/** A field without a width, for rows that size their own fields. */
export const fieldBase =
    'min-w-0 rounded-lg border border-control bg-transparent px-3 py-2 outline-none focus:border-ink transition-colors';

export const fieldClass = `w-full ${fieldBase}`;
export const labelClass = 'block text-sm font-bold uppercase tracking-widest text-muted mb-2';
